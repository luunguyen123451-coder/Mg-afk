#!/bin/sh
# Gradle wrapper stub - download the real wrapper or use Android Studio
echo "Please open this project in Android Studio, which will download Gradle automatically."
echo "Or run: gradle wrapper --gradle-version 9.3.1"
exit 1
